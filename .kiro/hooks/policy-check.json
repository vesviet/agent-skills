{
  "name": "Policy Check (agent-skills)",
  "version": "1.0.0",
  "description": "Check tool action against action-boundaries.yaml before write, shell, or delete operations. Advisory mode: warns on requires_approval, blocks on denied.",
  "when": {
    "type": "preToolUse",
    "toolTypes": ["write", "shell"]
  },
  "then": {
    "type": "runCommand",
    "command": "AGENT_SKILLS_ROOT=\"${AGENT_SKILLS_ROOT:-$PWD}\" AGENT_ACTIVE_ROLE=\"${AGENT_ACTIVE_ROLE:-agent-coordinator}\" python3 \"${AGENT_SKILLS_ROOT:-$PWD}/core/scripts/hooks/check-policy.py\""
  }
}
