{
  "name": "Trace Span Logger (agent-skills)",
  "version": "1.0.0",
  "description": "Append lightweight JSONL trace span to core/observability/spans/ after each tool call for agent observability and session replay.",
  "when": {
    "type": "postToolUse",
    "toolTypes": ["*"]
  },
  "then": {
    "type": "runCommand",
    "command": "AGENT_SKILLS_ROOT=\"${AGENT_SKILLS_ROOT:-$PWD}\" AGENT_ACTIVE_ROLE=\"${AGENT_ACTIVE_ROLE:-agent-coordinator}\" python3 \"${AGENT_SKILLS_ROOT:-$PWD}/core/scripts/hooks/log-trace-span.py\""
  }
}
