interface:
  display_name: "Agent Delegation"
  short_description: "Delegate scoped sub-tasks from a supervisor agent to specialist worker agents..."
  default_prompt: "Use $agent-delegation to execute agent delegation tasks in this codebase."
