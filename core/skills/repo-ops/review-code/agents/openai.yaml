interface:
  display_name: "Review Code"
  short_description: "Two-axis review of a diff: standards and spec"
  default_prompt: "Use $review-code to review the changes since <fixed-point> on both axes — standards compliance and spec fidelity."
