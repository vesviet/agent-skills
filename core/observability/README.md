# Agent Observability Spans

Local JSONL trace logs written by `core/scripts/hooks/log-trace-span.py` when Cursor hooks are enabled.

Schema: `contracts/schemas/agent-trace-span.json`

Do not commit span files — they may contain paths from active sessions.
