## 2026-06-21T02:45:02Z

You are a teamwork_preview_worker. Your task is to modify the target file `core/skills/foundation/incident-report/SKILL.md` in the active worktree to add the requested updates based on the gap analysis.

Target file physical path:
`/System/Volumes/Data/Users/tuananh/.gemini/antigravity/brain/62e31824-50da-490d-b94b-5047049035f1/.system_generated/worktrees/subagent-Skill-Upgrader-teamwork-preview-23882b8f/core/skills/foundation/incident-report/SKILL.md`

⚠️ IMPORTANT SYSTEM RULE:
When modifying files inside the repository (workspace), do NOT pass the `ArtifactMetadata` parameter. `ArtifactMetadata` is ONLY for coordination/state files inside the agent's folder (artifacts). If you pass `ArtifactMetadata` when editing files in the workspace repository, the tool will error with "files must be written to the correct artifact directory".

Please make the following additions:
1. In the `## Core Rules` section, append these three rules after the existing list items (specifically after the line `- use `contracts/schemas/incident-report.json` for structured handoff to Agent Coordinator or SRE`):
```markdown
- categorize AI/LLM system-specific incidents using a dedicated taxonomy (such as hallucination-driven incorrect outputs, tool-call chain corruption, or prompt injection unauthorized actions)
- treat AI-assisted RCA (from tools like FireHydrant AI, PagerDuty SRE Agent, or Datadog Bits AI) as a starting hypothesis requiring human verification — never as a final conclusion
- require a replayable agent audit trail (full chain-of-thought, tool calls, and policy checks) as a mandatory category of postmortem evidence
```

2. In the `## Suggested Process` section, append this new H3 section before `## Output Format`:
```markdown
### 6. 2026: AI/LLM Incident Management

- **AI/LLM-Specific Incident Taxonomy**: Classify failures unique to AI-driven systems:
  - *Hallucination-driven failures*: Incorrect or misleading recommendations, database updates, or client payloads generated by the model.
  - *Tool-call chain corruption*: Malformed tool payloads, unexpected API inputs, or loops/hangs in agentic tool-execution pipelines.
  - *Prompt injection / Security bypass*: Unauthorized actions, data exposure, or policy violations caused by malicious user prompts.
- **AI-Assisted Root Cause Analysis (RCA)**: When using AI operational assistants (such as FireHydrant AI, PagerDuty SRE Agent, or Datadog Bits AI) to compile SRE timelines or synthesize RCA findings:
  - Treat all AI-generated findings as starting hypotheses only.
  - Require a human engineer to verify all timelines, log matches, and causal links before finalizing the report.
- **Replayable Agent Audit Trail**: Ensure that all postmortem reviews for agent-related incidents contain a complete, replayable history of the agent's execution. This includes the model version, temperature, exact system prompts, detailed chain-of-thought traces, inputs/outputs of all tool calls, and security policy checks.
```

3. In the `## Checklist` section, append these three checklist items after the existing items (specifically after the line `- [ ] incident-report.json emitted if structured handoff required`):
```markdown
- [ ] AI/LLM-specific taxonomy applied to classify hallucination, tool-call, or prompt injection incidents
- [ ] AI-assisted RCA findings validated and confirmed by a human operator
- [ ] replayable agent audit trail (prompts, tool calls, policy logs) attached as postmortem evidence
```

Preserve all other existing text, formatting, and the frontmatter exactly as they are. Do not delete or rewrite any other sections.

After implementing these modifications, run the validation tool to ensure the changes are valid:
`python3 core/scripts/validate-skills.py`
from the project root directory `/System/Volumes/Data/Users/tuananh/.gemini/antigravity/brain/62e31824-50da-490d-b94b-5047049035f1/.system_generated/worktrees/subagent-Skill-Upgrader-teamwork-preview-23882b8f`.

Write your handoff report to `handoff.md` in your own folder. Report the exact commands run, the validation output, and details of the edits.
