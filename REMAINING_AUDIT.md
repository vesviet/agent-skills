# Remaining Upgrade Audit (post-skills/roles/policies/workflows pass)

**Scope:** every directory in the pack that has NOT been upgraded to Standard 2026 yet.
**Baseline:** `validate-all.py` currently passes 16/16 validators, 0 errors, 0 warnings.
**Goal:** identify the remaining gaps and decide whether they need a 2026 upgrade pass or are already correct.

## 0. Recap — what is already upgraded

| Area | Status | Coverage |
| --- | --- | --- |
| `core/skills/` (97) | upgraded | 100% have `## Failure Modes`, 97% mention OWASP, 96% have `## Output Contracts` |
| `core/roles/` (34) | upgraded | 100% have `## Failure Modes`, 100% OWASP, 100% contract refs |
| `core/policies/` (3 + README) | upgraded | explicit `denied` for non-owner roles, T1/T2 fixes, audit emission flag |
| `core/workflows/` (18) | upgraded | 100% have `### Failure Modes` + `### Output Contracts`; T1 (10) also `### Security Guardrails` |
| 4 audit deliverables in repo | written | `AUDIT.md`, `ROLES_AUDIT.md`, `POLICIES_AUDIT.md`, `WORKFLOWS_AUDIT.md` |

## 1. Inventory of remaining areas

| Area | Files | Validator | Current pass | Upgrade gap |
| --- | --- | ---: | --- | --- |
| `core/rules/code.md` | 1 (23 lines) | `validate-rules.py` | pass | no Failure Modes / OWASP — but rules are intentionally terse and META-RULE-driven; **out of scope** |
| `core/contracts/README.md` | 1 (116 lines) | `validate-contracts.py` (43 schemas) | pass | good already; only 1 schema mentions OWASP, 8 mention failure, 10 mention security — schemas are user-defined JSON, not the place to enforce prose |
| `core/contracts/schemas/*.json` | 43 schemas | `validate-contracts.py` | pass | 1 schema mentions OWASP/ASI; could add ASI section to `incident-report.json` and `ai-risk-register.json` — **optional, low value** |
| `core/observability/otel-genai.md` | 1 (206 lines) + README | none (not a validator target) | n/a | comprehensive; **no upgrade** |
| `core/prompts/golden/` | 1 manifest + 18 cases | n/a | pass | small but functional; **no upgrade** |
| `core/a2a/registry/*.agent-card.json` | 34 generated | `validate-agent-cards.py` | pass | generated from roles, not hand-edited; **out of scope** |
| `core/a2a/.well-known/*.json` | 3 files (registry, agent, ai-catalog) | `validate-a2a-compliance.py` | pass | generated; **out of scope** |
| `core/config/environment.sh` + README | 2 files | n/a | pass | minimal, env vars only; **no upgrade** |
| `core/vs-code/.vsconfig.yaml` | 1 file | n/a | pass | VSCode config only; **no upgrade** |
| `core/codex/` | 2 files | n/a | pass | Codex adapter README; **no upgrade** |
| `adapters/antigravity/` | 2 md + 3 other | `validate-rules.py` (partial) | pass | needs a one-line "Last updated" check; **out of scope this pass** |
| `adapters/claude/` | 1 md | `validate-rules.py` | pass | small; **no upgrade** |
| `adapters/cursor/` | 1 md + 1 other | `validate-rules.py` | pass | small; **no upgrade** |
| `overlays/` (17 dirs) | 10 skills, 5 roles, 11 workflows | `validate-overlays.py` | pass | 10 skills don't have Failure Modes yet — **Tier 1 upgrade target** |
| `packs/` (13 dirs) | 0 .md (only configs) | `validate-packs.py` | pass | empty pack docs; **out of scope** |
| Root `*.md` | README, AGENTS, CHANGELOG, CLAUDE, CONTRIBUTING, INDEX, USER_GUIDE_v2 | none | n/a | INDEX/CHANGELOG counts may be stale after the upgrades; **optional update** |
| `.agents/`, `.cursor/`, `.kilocode/`, `.kiro/`, `.github/`, `kilo-code/`, `.cursorrules`, `.editorconfig`, `.gitattributes`, `.gitignore` | 1-3 files each | n/a | pass | adapter boilerplate; **no upgrade** |

## 2. Per-area detail

### 2.1 `core/rules/code.md` — **NO UPGRADE**

23 lines of META-RULE + commit policy + comment hygiene. Adding `## Failure Modes` here would dilute the always-on trigger. Already references `core/policies/action-boundaries.yaml` and `core/policies/data-classification.yaml` (the policies I just upgraded). **Skip.**

### 2.2 `core/contracts/` — **OPTIONAL**

- README is comprehensive (116 lines) and points to the schema index
- 43 JSON schemas are validated
- Only `ai-risk-register.json` mentions OWASP/ASI (1 of 43). Could add ASI items to `incident-report.json` and `ai-risk-register.json` schema enums — **low value**
- **Recommend:** skip in this pass; schemas are user-defined JSON and prose would clutter them

### 2.3 `core/observability/otel-genai.md` — **NO UPGRADE**

206 lines, comprehensive OTel GenAI semantic conventions guide, 2026-current. **Already at Standard 2026.**

### 2.4 `core/prompts/` — **NO UPGRADE**

1 manifest (agent-coordinator-phase-gate) + 18 cases. Functional. **No upgrade needed.**

### 2.5 `core/a2a/` registry + `.well-known/` — **NO UPGRADE**

Generated by `generate-a2a-registry.py` from role files. The roles are already upgraded, so the next regeneration will reflect the changes. **No hand-edits needed.**

### 2.6 `core/codex/`, `core/config/`, `core/vs-code/` — **NO UPGRADE**

Adapter/config files. Minimal, out of scope for a prose upgrade.

### 2.7 `adapters/` — **NO UPGRADE**

Adapter READMEs. Out of scope for the Standard 2026 prose pass. The 2026 standards are already embedded in role + skill + policy + workflow files.

### 2.8 `overlays/` — **T1 UPGRADE TARGET**

17 overlay directories. Stats:

- 10 SKILL.md across 6 overlays (`golf-icm`, `icm-main`, `laravel-filament`, `lease-content`, `maydiengiaisaigon`, `obj-configurator`, `r3f-stack`, `vesviet-content`)
- 5 roles across 3 overlays (`laravel-filament`, `lease-content`, `maydiengiaisaigon`, `vesviet-content`)
- 11 workflows across 6 overlays (`ecommerce-microservices`, `laravel-filament`, `lease-content`, `maydiengiaisaigon`, `vesviet-content`)

**Gap:** 10 overlay skills likely don't have `## Failure Modes` or OWASP references (mirrors the core skills pre-upgrade). Roles and workflows in overlays likely don't either.

**Recommended upgrade scope (Tier 1):**
- 10 overlay skills — add `## Failure Modes` + OWASP + Output Contracts (mirroring core pattern)
- 5 overlay roles — add `## Failure Modes`
- 11 overlay workflows — add `### Failure Modes` + `### Output Contracts`

### 2.9 `packs/` — **NO UPGRADE**

13 pack directories; only config files, no `.md` content. **No upgrade needed.**

### 2.10 Root `*.md` — **OPTIONAL**

- `INDEX.md` (53 KB) — has the catalog of 34 roles / 107 skills / 18 workflows / 43 contracts. **Counts are still correct** (no new files added). The "2026 Standards" content at the bottom of skills/roles may not be reflected in the index headers, but the index is auto-regenerated. Optional update.
- `CHANGELOG.md` (52 KB) — pre-existing changelog. After the 4 upgrade passes, a new changelog entry would be appropriate. **Optional update** (not a validator target).
- `AGENTS.md`, `CLAUDE.md`, `CONTRIBUTING.md`, `README.md`, `USER_GUIDE_v2.md` — stable; references to "OWASP ASI" can be added in a future doc refresh, but currently pass the rules validator.

## 3. Recommendation summary

### T1 — apply now (overlays)

Mirror the core upgrades in the 10 overlay skills + 5 overlay roles + 11 overlay workflows. This is the only remaining substantive gap.

### T2 — optional (root docs + contracts)

- Add a changelog entry summarizing the 4 upgrade passes
- Optionally inject ASI enum values into `incident-report.json` and `ai-risk-register.json` schemas
- Refresh `INDEX.md` "Failure Modes" / "OWASP ASI" footer pointers (low value)

### T3 — skip (already at Standard 2026 or out of scope)

- `core/rules/code.md` — META-RULE is intentional
- `core/observability/otel-genai.md` — comprehensive
- `core/prompts/` — functional
- `core/a2a/registry/`, `.well-known/` — generated
- `core/codex/`, `core/config/`, `core/vs-code/` — adapter boilerplate
- `adapters/` — adapter READMEs
- `packs/` — config-only

## 4. Out of scope (this audit only — flag if user wants)

- New overlays (out of scope; the audit is for upgrades).
- Removing overlays (out of scope).
- Renaming files (out of scope).
- Updating the validators (out of scope; the audit is data-only).
- Cross-pack index regeneration (T2 follow-up).

## 5. Questions for User Before Apply

1. **Overlay scope:** apply the Standard 2026 upgrade to the 10 overlay skills + 5 overlay roles + 11 overlay workflows (mirroring core)? Or leave overlays untouched (they're optional repo-specific)?
2. **Changelog entry:** add a summary entry to `CHANGELOG.md` for the 4 upgrade passes (T1 + T2 skills, T1-T3 roles, T1-T2 policies, T1-T3 workflows)?
3. **INDEX refresh:** re-run `generate-index.py` to refresh counts and pointers in `INDEX.md`? (Counts are already correct, but the file is hand-curated.)
4. **Contract ASI enums:** add OWASP ASI items as enum values to `incident-report.json` and `ai-risk-register.json` schemas? (Low value; schemas are user-defined JSON.)
5. **Commit/push policy:** per `core/rules/code.md` META-RULE — do NOT commit unless user confirms. Default: edits applied, validators re-run, no commit.

---

*Awaiting user direction before applying changes.*
